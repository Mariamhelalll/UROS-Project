# UROS-Project
To run the document:
git status

git add .              # stage everything
git add path/to/file   # stage a specific file

git commit -m "Fix double-sigmoid bug in UNet3D training"

git push origin main   # or whatever your branch is called